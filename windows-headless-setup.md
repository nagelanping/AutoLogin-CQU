# Windows 无头运行配置指南（任务计划程序）

将 `AutoLogin-CQU.exe` 配置为后台进程：登录后自动启动、无窗口、无托盘。使用 Windows 自带的任务计划程序，无需第三方软件，无需管理员权限。

配置完成后的行为：

- 登录 Windows 后自动在后台启动，不显示窗口；
- 网络故障期间按 `CHECK_INTERVAL` 周期自动重试，恢复后自动保活；
- 持续运行直至注销或关机。

## 前置检查

无头运行不产生任何可见输出。必须先确认前台运行正常，再进行后台配置，否则出错时无法定位原因。

1. 建立固定目录，下文以 `<程序目录>` 表示（例如 `D:\AutoLogin-CQU\`），放入两个文件：

   - `AutoLogin-CQU.exe`
   - `config.yaml`（已填写学号、密码，格式参考 `src/windows/config.yaml` 的注释）

   两文件必须位于同一目录，程序从 exe 所在目录读取 `config.yaml`。
2. 双击 `AutoLogin-CQU.exe`，窗口应显示：

   ```text
   === CQU 自动登录服务已启动 ===
   按 Ctrl+C 或关闭窗口可安全退出。
   按 Ctrl+P 暂停/继续服务。
   ```

   之后默认每 20 秒输出一行结果，如 `[成功] 设备已在线 (...)` 或 `[失败] 登录失败 (...)`。
3. 若窗口显示错误（如 `未配置有效账号或密码`），先修正 `config.yaml`；窗口停在报错时按回车关闭。
4. 按 `Ctrl+C` 退出程序，并在任务管理器中确认无 `AutoLogin-CQU` 进程。同一时间只能运行一个实例。

## 第 1 步：打开任务计划程序

1. 按 `Win + R` 打开"运行"对话框。
2. 输入 `taskschd.msc`，回车。

## 第 2 步：创建任务

点窗口右侧的"创建任务(T)…"。不要使用"创建基本任务"（缺少所需选项）。在弹出的对话框中按顺序配置以下标签页。

## 第 3 步："常规"标签页

1. "名称"：`AutoLogin-CQU`。
2. 选 **"只在用户登录时运行"**：无需密码，且校园网本身在登录后才连接。
   （"不管用户是否登录都要运行"要求输入账户密码，且在未登录的会话中运行，本场景不需要。）
3. 勾选 **"隐藏运行"**：程序运行时不显示窗口。
4. "配置为"保持默认。

## 第 4 步："触发器"标签页

1. 点"新建(N)…"。
2. "开始任务"：**"登录时"**。
3. "用户"：选"特定用户(U)…"，点"更改(C)…"，选中当前账户；点"检查用户名称"，确认显示绿色"✓ 这是一个有效的用户名称"后点"确定"。
4. "任务名称"留空，点"确定"。

   触发器列表应显示：`登录时 → AutoLogin-CQU → 特定用户`。

## 第 5 步："操作"标签页

1. 点"新建(N)…"。
2. "操作"：**"启动程序"**。
3. "程序或脚本(P)"：浏览选择 `<程序目录>\AutoLogin-CQU.exe`。
4. "起始于(可选)(A)"：`<程序目录>`（不带结尾反斜杠）。
5. "添加参数(V)"留空，点"确定"。

   操作列表应显示：`启动程序 → <程序目录>\AutoLogin-CQU.exe`。

## 第 6 步："条件"标签页

1. **取消勾选"仅在此计算机使用交流电源时启动任务"**（笔记本电池供电也需运行）。
2. 其余保持默认。

## 第 7 步："设置"标签页

1. **确认"如果任务超过以下时间仍在运行，停止任务"未勾选**。若勾选，程序运行满 1 天会被系统终止；本程序为长驻进程，该项必须不勾选。
2. **确认"如果任务已成功完成超过以下时间，删除该任务"未勾选**。
3. 建议勾选"如果任务失败，每 _ 重新启动"，间隔选"1 分钟"，"尝试次数"填 `3`：程序异常退出后 1 分钟自动重启，最多 3 次。
4. 其余保持默认。

## 第 8 步：保存与验证

1. 点"确定"保存（本方案不要求输入密码）。
2. 在任务列表找到 `AutoLogin-CQU`，确认"状态"列为空（尚未启动）。
3. 右键任务 → "运行(R)"。
4. 确认"状态"列显示"正在运行"。
5. 验证校园网可正常访问。
6. 需立即保持运行则不做其他操作：程序会持续运行至注销，下次登录自动重新启动；需停止则右键任务 → "结束(E)"。

## 确认运行状态

- 任务计划程序中 `AutoLogin-CQU` 的"状态"列显示"正在运行"；
- 或任务管理器（`Ctrl + Shift + Esc`）的"详细信息"选项卡中存在 `AutoLogin-CQU.exe` 进程。

## 停止、修改与删除

- **停止**：任务计划程序中右键任务 → "结束(E)"。
- **删除**：右键任务 → "删除(D)"；不影响 exe 与 `config.yaml` 文件。
- **修改配置**（学号、密码、周期）：用记事本编辑 `<程序目录>\config.yaml` 并保存。程序仅在启动时读取一次配置，修改后须"结束"任务再"运行"方可生效。
- **升级 exe**：覆盖新 exe 后"结束"任务再"运行"。

## 无头运行的限制

- 无控制台窗口，`Ctrl+P` 暂停、托盘图标、最小化均不可用；
- 默认不产生可见日志。排查时：右键任务 → "属性" → "常规" → 临时取消勾选"隐藏运行" → "确定" → 右键"运行"，窗口即显示输出；排查完毕后重新勾选。

## 故障排查

查看任务列表"上次结果"列（右键任务 → "属性" → "常规"底部亦显示）：

| 上次结果                      | 含义                           | 处理                                                                      |
| ----------------------------- | ------------------------------ | ------------------------------------------------------------------------- |
| `0x0`                       | 正常                           | 无需处理                                                                  |
| `78`（可能显示为 `0x4E`） | `config.yaml` 缺失或内容无效 | 在目录中前台运行 exe 一次，窗口停住显示具体错误；修正配置后右键任务"运行" |
| 状态一直为空                  | 未触发                         | 确认已登录 Windows；"登录时"触发不会在注销状态下启动                      |

其他情况：

- **登录后未自动启动**：检查触发器中"登录时"条目是否存在、用户是否为当前账户。
- **任务"正在运行"但校园网不通**：临时取消"隐藏运行"后手动运行，查看结果行；`[失败] 登录失败` 行含地址来源（`manual`/`route`/`heuristic`），结合 `config.yaml` 的 `SERVER_IP`/`LOGIN_IP` 排查。
- **多实例**：在任务管理器中结束多余的 `AutoLogin-CQU.exe` 进程，再"运行"任务。
- **已勾选"隐藏运行"但仍弹出窗口**：系统默认终端应用为 Windows Terminal 时，隐藏标志不生效（WT 会为每个新控制台进程开窗口）。两种处理：
  1. 将默认终端应用改为"Windows 经典"：Windows Terminal（`Win + T`）→ `⋯` → 设置 → 常规 → "默认终端应用" → "Windows 经典"；或
  2. 任务改用"不管用户是否登录都要运行"（常规页第一项，保存时需输入账户密码）：进程运行在非交互会话，不创建可见控制台。
