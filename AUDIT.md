# AutoLogin-CQU 代码审计与修改建议

> 审计范围：仓库当前提交中的 C++ Linux、C++ Windows、配置文件和 systemd 部署文件。
>
> Python 版本不再维护；请勿查看或改动其归档内容。
>
> 结论分为两类：
> - **复杂度审计**：哪些代码可以删除、合并或改用系统能力。
> - **实现风险审计**：已经影响安全性、正确性、可维护性或部署可靠性的硬伤。
>
> 本文只给出修改建议，不直接改动实现。

## 1. 总体判断

项目的真实核心非常小：

1. 读取账号和运行参数；
2. 获取本机 IPv4/IPv6；
3. 解析认证服务器地址；
4. 构造校园网门户请求；
5. 识别“登录成功 / 已在线 / 失败”；
6. 按周期重复，并响应退出信号。

当前仓库维护两套平台实现：

- `src/linux/AutoLogin-CQU.cpp`：libcurl + POSIX API；
- `src/windows/AutoLogin-CQU.cpp`：WinHTTP + Win32 托盘、键盘和窗口线程。

同一个业务规则在两处复制并逐渐漂移：配置键、响应判断、URL 参数、TLS 行为、IP 选择和错误处理都可能不一致。Windows 版本的外围交互功能远大于核心网络功能，维护成本主要消耗在托盘和多线程生命周期，而不是登录协议本身。

### 建议的目标架构

保持“平台适配”和“业务协议”分离，但不要建立过度抽象的框架：

```text
配置文件
   ↓
配置校验与运行参数
   ↓
平台适配层
  ├─ Linux：libcurl、getifaddrs、POSIX signal
  └─ Windows：WinHTTP、IP Helper、Win32 wait/event
   ↓
统一的请求模型与响应分类
   ↓
一个最小的周期调度循环
```

具体建议：

- **中期**：把“配置校验、URL 参数、响应分类、重试策略”收敛为一份协议定义；平台代码只负责 DNS/IP、HTTP 请求和进程生命周期。
- **不建议**：为了复用几十行字符串处理代码，引入工厂、接口层、依赖注入容器或跨平台网络库。这个项目不需要这些层。
- **Windows 托盘**应作为可选 UI 外壳，不能和登录主循环、网络状态、退出清理互相耦合。

## 2. 优先级最高的硬伤

### P0：关闭了 TLS 证书验证

**位置**：

- `src/linux/AutoLogin-CQU.cpp` 的 `PerformLogin` 初始化部分；
- `src/windows/AutoLogin-CQU.cpp` 的 `PerformLogin`。

Linux 显式设置：

```cpp
CURLOPT_SSL_VERIFYPEER = 0
CURLOPT_SSL_VERIFYHOST = 0
```

Windows 显式忽略未知 CA、域名不匹配和证书过期。

这使账号密码可以被中间人代理读取。尤其当前代码连接的是 `SERVER_IP` 或解析后的 IP，再手动添加 `Host: login.cqu.edu.cn`，证书校验和连接地址本来就容易不匹配，不能用“关闭校验”解决。

**建议修改顺序**：

1. 保持请求 URL 使用 `https://login.cqu.edu.cn:802/...`；
2. 使用 libcurl 的 `CURLOPT_RESOLVE` 或等价的受控解析方式把域名映射到指定 IP，让 TLS SNI 和证书主机名仍然是 `login.cqu.edu.cn`；
3. Windows 优先让 WinHTTP 使用域名连接；如必须指定 IP，确认 WinHTTP 的 SNI/证书主机名配置方式后再实现；
4. 恢复系统 CA 和主机名校验；
5. 只有在用户明确选择“不安全调试模式”时才允许绕过校验，并且默认关闭，日志中必须明确警告。

如果门户确实只支持异常证书，应该记录为部署限制并提供受控 CA 配置，而不是默认关闭验证。

### P0：账号密码通过 GET 查询参数发送

**位置**：两个实现的 URL 构造函数。

`user_password` 被拼进 URL。即使程序自身不打印完整 URL，URL 也可能进入代理、网关、HTTP 调试日志、系统监控或 libcurl/WinHTTP 的诊断日志。

**建议**：

- 先确认 CQU 门户是否强制要求 GET；
- 如果协议允许，改为 POST body；
- 如果协议只能 GET，至少做到：不记录完整 URL、不在异常信息中输出 URL、避免代理环境、限制日志、在文档中明确风险；
- 配置文件权限继续按 systemd 文档设置为专用用户可读，Windows 侧也应补充 ACL 建议。

这是协议兼容性问题，不能只凭代码推测 POST 一定可用，修改前应使用真实门户或抓包验证。

### P1：配置文件被称为 YAML，但实际是自定义的单层键值解析器

**位置**：

- Linux `LoadConfig`；
- Windows `LoadYamlConfig`；
- `src/linux/config.yaml`、`src/windows/config.yaml`。

解析器只支持“每行第一个冒号分隔的键和值”，并不会解析 YAML。当前实现存在这些行为：

- 缩进、嵌套、列表和 YAML 转义不支持；
- 非法行被静默跳过；
- 未知键被静默接受；
- 重复键没有报错；
- 数值非法时回退默认值，用户可能误以为配置生效；
- IP 地址没有校验；
- Linux 与 Windows 的配置校验规则不完全一致。

**推荐的最小方案**：不要引入完整 YAML 库，直接把格式改名为项目自定义的 `KEY=value` 或 `KEY: value` 配置，并在文档中明确“仅支持单层键值”。启动时对必需项、未知项、重复项、非法数值和非法 IP 明确报错并返回 `78`。

如果必须兼容真正的 YAML，再统一引入一个经过支持的平台库，并让两套 C++ 共享同一套配置行为。不要继续扩展当前的半成品 YAML 解析器。

## 3. 业务正确性和网络处理问题

### 3.1 响应解析依赖字符串片段，容易误判

**位置**：

- Linux `ClassifyLoginResponse`；
- Windows `ContainsJsonIntField` / `ClassifyLoginResponse`。

Linux 直接搜索 `"result":1`、`"ret_code":2` 和固定文本。Windows 虽然写了一个整数提取器，但仍不是 JSON 解析器，并且对截断响应、转义、字段嵌套和格式变化都不稳健。

**建议**：

- 先限制响应体大小，这一点已有；
- 再实现一个只读取门户所需顶层字段的严格解析器，至少验证字段边界、引号、冒号和数值；
- 明确未知响应属于 `Failed/Unknown`，不要把“包含某个片段”当作成功；
- 将响应分类做成纯函数，提供离线样例测试：成功、已在线、失败、截断、字段顺序变化、空响应、HTML 错误页。

不建议为两个字段引入完整 JSON 框架，除非实际响应已经超出一个小型严格解析器的能力。

### 3.2 DNS 解析绕过了 HTTP 超时

**位置**：Linux `ResolveHostToIP`、Windows `ResolveHostToIP`，以及配置说明中的 `TIMEOUT`。

代码先调用系统 `getaddrinfo`，再发起 HTTP 请求。`TIMEOUT` 只限制后续 libcurl/WinHTTP 阶段，不能可靠限制系统 DNS 解析耗时。文档已经部分承认这一点，但程序仍会在网络不可用时被 DNS 阻塞。

**建议**：

- 优先让 HTTP 客户端使用域名完成解析和连接；
- 需要固定服务器 IP 时使用客户端提供的域名到 IP 映射能力，而不是自己先解析再把 IP 当主机名；
- 如果保留同步 `getaddrinfo`，至少在文档和日志中把它视为独立阶段，不要声称 `TIMEOUT` 覆盖整个请求；
- 不要为了实现异步 DNS 引入复杂线程池，除非实际有长期阻塞证据。

### 3.3 Linux IP 选择逻辑是启发式的，接口命名不可依赖

**位置**：Linux `GetLocalIPs`。

代码通过接口名前缀 `eth`、`en`、`wlan`、`wl` 判断物理网卡，并把 `192.168.x.x` 降为最低优先级。现代 Linux 可能使用其他接口命名，也可能存在容器、VPN、桥接、隧道、校园网拨号接口。最终选中的地址不一定是访问认证门户实际使用的源地址。

**建议**：

- 最可靠的选择是根据到认证服务器的实际路由确定源地址；
- 如果不想引入复杂路由查询，保留当前启发式但增加 `LOGIN_IP`，并在日志中输出“自动选择 / 手动指定”；
- 将地址过滤规则改为明确的可配置策略，不要继续堆叠硬编码网段；
- IPv6 只要门户需要就验证其可达性，否则不要把“找到一个非链路本地 IPv6”当成可用地址。

Windows 的地址选择也应与 Linux 的语义保持一致，不能只保证函数名称相同。

### 3.4 固定周期请求，没有区分网络状态和失败类型

两个版本都会按固定周期发请求。DNS 失败、TLS 失败、HTTP 500、认证失败、已经在线，最终都只是下一周期继续尝试；systemd 还可能在进程级别额外重启。

**建议**：

- 保留固定周期作为默认，避免引入复杂调度器；
- 将结果至少分成“已在线/成功”“认证失败”“暂时网络错误”“配置错误”；
- 配置错误立即退出，凭据错误可以降低重试频率，网络错误按固定周期重试；
- 只对短时间连续网络故障增加一个有上限的退避，不要无限指数退避导致用户误判服务已经停止。

### 3.5 HTTP 状态码只接受 200，且没有检查响应内容类型

**位置**：Linux `PerformLogin`、Windows `PerformLogin`。

门户可能返回重定向、错误页或代理页。当前实现把非 200 直接当作无效，而对 200 的任意内容继续做片段判断。

**建议**：记录状态码、最终 URL/请求阶段和响应类型（但不要记录含密码的 URL），对重定向策略做明确选择；对非门户 HTML 或空响应归类为网络/门户异常，而不是普通登录失败。

## 4. Windows 实现的复杂度和生命周期风险

### 4.1 托盘、键盘监听和登录逻辑耦合过深

**位置**：`src/windows/AutoLogin-CQU.cpp` 的全局事件、托盘函数、`ConsoleMonitorThread`、`MessageLoopThread`、`KeyboardMonitorThread` 以及 `main`。

核心需求是后台周期请求，但 Windows 版本同时维护多个线程、Win32 窗口、托盘图标、暂停事件和键盘快捷键。大量全局句柄和原子变量让退出路径变得难以验证。

**建议的最小重构**：

1. 先让登录服务只依赖一个退出事件和一个暂停状态；
2. 将托盘 UI 放到独立模块，UI 只发送 `pause/exit` 事件；
3. 默认没有托盘能力时，不创建相关线程；
4. 如果托盘不是明确需求，直接删除托盘和全局快捷键，保留 Ctrl+C/关闭窗口退出。

这是本仓库最明显的过度实现区域。它没有提高认证协议的可靠性，却显著增加了线程和句柄清理风险。

### 4.2 清理阶段等待线程超时后仍然关闭句柄

**位置**：Windows `main` 的 `cleanup`。

代码对控制台监控、键盘监控和消息线程分别等待 `500ms` 或 `1000ms`，超时后仍直接 `CloseHandle`。这并不保证线程已退出；如果线程仍在使用全局窗口、事件或消息队列，可能出现退出时竞态或资源访问问题。

**建议**：

- 线程函数必须有明确的退出条件；
- 清理时发送退出事件/消息后等待线程真正结束；
- 超时只能记录错误，不能假定线程安全退出；
- 关闭线程句柄与销毁线程依赖的窗口、事件必须有固定顺序；
- 逐项检查 `CreateThread` 失败后的部分初始化路径。

### 4.3 `GetModuleFileNameA` 对路径长度的处理过于保守

**位置**：Windows `LoadConfig`。

使用固定 `MAX_PATH` 缓冲区，返回长度等于 `MAX_PATH` 时直接失败。长路径环境下程序无法启动，即使系统和文件都支持长路径。

**建议**：使用动态缓冲区循环调用，或统一使用宽字符 API 与长路径处理。若项目明确只支持普通路径，应在文档中写明，而不是静默把长路径当作配置读取失败。

### 4.4 WinHTTP 和 libcurl 的行为没有统一

两套实现对代理、TLS、连接复用、超时、响应解析和日志的处理不一致。例如 Linux 设置了 `CURLOPT_FRESH_CONNECT` / `CURLOPT_FORBID_REUSE`，Windows禁用了 keep-alive。应先写出协议级行为表，再决定哪些差异是平台必要的，哪些只是历史遗留。

## 5. Linux C++ 的实现问题

### 5.1 忽略了关键 libcurl 返回值

**位置**：Linux `main` 和 `PerformLogin`。

大量 `curl_easy_setopt`、`curl_global_init` 和相关初始化调用没有检查返回值。初始化失败、选项不支持或句柄状态异常时，程序可能继续运行并把真正原因推迟到请求阶段。

**建议**：

- 初始化阶段至少检查 `curl_global_init`、`curl_easy_init`、header 创建和所有关键选项；
- 对非关键优化选项可以记录警告并继续；
- 把重复的 `setopt + 检查` 收敛成一个很小的本地辅助函数，不能为此创建通用配置框架。

### 5.2 配置数值“非法即静默回退”不利于运维

**位置**：Linux `ParsePositiveLong`、Windows `GetConfigSeconds`。

配置写错时回退默认值，服务可以继续运行，但用户可能不知道 `TIMEOUT=abc` 没有生效。这类行为在后台服务中会制造隐蔽故障。

**建议**：对关键配置分开处理：

- 未填写可使用默认值；
- 填写但格式非法应明确报错并返回 `78`；
- 超出范围应打印实际范围；
- 启动日志只打印非敏感的有效配置摘要。

### 5.3 `WriteCallback` 的截断语义需要明确

**位置**：Linux `WriteCallback`。

回调把最多 `4096` 字节存入响应，但仍返回全部收到的 `bytes`，这表示“数据已被成功消费”，因此不会触发 libcurl 的写入错误。这个行为本身可以是有意的，但分类器无法知道响应是否被截断。

**建议**：增加一个 `truncated` 标志；一旦达到上限，后续只消费但把结果标记为未知，不要把截断响应误判为登录失败或成功。

### 5.4 `using namespace std` 和全局可变配置降低局部可验证性

**位置**：Linux、Windows 源文件顶部及全局变量区。

这不是当前最严重的问题，但全局 `STUDENT_ID`、`USER_PASSWORD`、`SERVER_IP` 等变量使加载配置、构造请求和测试强耦合。

**建议**：用一个简单的 `Config` 结构按值传递，先替换全局配置变量；不要进一步引入配置服务、单例或依赖注入。

## 6. 配置、systemd 和运维问题

### 6.1 配置模板仍含真实格式的占位凭据

`config.yaml` 中的 `xxxxxxxx` / `xxxxxx` 会被 Windows 特殊识别，但 Linux 只检查非空，因此 Linux 可能把模板值当作有效账号继续请求。

**建议**：统一两端的占位符检查；模板值应导致明确的配置错误，而不是向门户发送请求。最好使用 `${REQUIRED}` 这类不会被误认为学号/密码的明显标记。

### 6.2 systemd service 是模板，但文件名看起来像可直接安装的 unit

`autologin-cqu.service` 含 `<USERNAME>`、`<PROGRAM_DIR>`，这是文档化模板，不是可直接启用的服务。

**建议**：

- 保留模板也可以，但在文件名或顶部注释中明确 `template`；
- 文档中明确“先替换占位符，再复制到 `/etc/systemd/system`”；
- 增加一个不含敏感值的安装脚本不是必需品，除非实际反复手工部署造成错误。

### 6.3 日志会输出响应片段

Linux 和 Windows 会打印响应片段。门户返回内容可能包含账号、IP、会话或错误上下文。

**建议**：默认只输出结构化结果、状态码和错误类别；详细响应仅通过显式调试开关启用，并做长度、字段和凭据脱敏。systemd 文档已经提醒隐私，但代码默认仍然输出过多。

### 6.4 缺少可重复的构建入口

当前只能依赖源文件注释中的手工 `g++` 命令。没有 Makefile、CMake、构建脚本或版本化的编译验证。

**建议的最小方案**：分别增加 Linux 和 Windows 的一条构建命令脚本，或在 README 中固定“工作目录、编译器、依赖和输出路径”。不建议立即引入大型跨平台构建体系；当需要打包发布、自动构建或 CI 时再使用 CMake。

## 7. 过度设计审计：按可删除复杂度排序

以下是按 Ponytail 审计格式列出的纯复杂度问题；安全性和正确性问题已在前文单独列出。

- `delete:` 删除 Windows 托盘、全局快捷键和窗口监控，先保留后台循环与 Ctrl+C/关闭窗口退出；核心功能不依赖 UI。[`src/windows/AutoLogin-CQU.cpp`]
- `shrink:` 把 Windows 的多个退出/暂停机制合并为一个退出 event、一个 pause 状态和一个主循环；替代当前多个全局句柄、原子变量和线程协调。[`src/windows/AutoLogin-CQU.cpp`]
- `yagni:` 不要为当前只有一个服务实例的项目增加工厂、插件系统、依赖注入或通用调度器；一个 `Config` 加一个平台请求函数足够。[整个 C++ 实现]
- `shrink:` 删除重复的自定义响应分类逻辑，统一为一个纯函数和一组离线响应样例；替代 Linux、Windows 两份字符串判断。[两个实现]
- `shrink:` 删除“伪 YAML”解析器的继续扩展；要么固定为单层键值格式，要么一次性使用正式解析库，不要维护半套 YAML 语法。[`LoadConfig` / `LoadYamlConfig`]
- `native:` 用 Win32 wait/event 和 libcurl/WinHTTP 已有的超时、连接管理能力，删除手写的多线程轮询和重复连接控制；平台 API 已经提供所需原语。[Windows / Linux 网络循环]
- `shrink:` 将全局配置变量收敛为一个小型 `Config` 值对象；替代散落的可变全局字符串和整数。[`src/linux/AutoLogin-CQU.cpp`、`src/windows/AutoLogin-CQU.cpp`]
- `delete:` 不要增加新的“网络在线检测”接口；当前登录请求本身已经是最有价值的门户探测，除非实测证明需要额外探测。[主循环]
- `yagni:` 不要为固定的两个响应字段引入完整 JSON 框架；先实现严格、有限的字段解析，只有门户响应复杂到无法维护时才加依赖。[响应分类]

## 8. 推荐修改顺序

### 第一阶段：先消除风险

1. 恢复 TLS 验证，并解决“连接 IP 与证书域名”的问题；
2. 统一并严格校验配置，模板凭据不能启动请求；
3. 默认停止输出响应正文；
4. 修正 Windows 线程退出和句柄清理；
5. 给响应分类补离线测试样例。

### 第二阶段：收敛实现

1. 写出 Linux/Windows 共同的协议行为表；
2. 统一配置键、默认值、范围和错误码；
3. 把 URL 构造、响应分类和脱敏日志规则收敛；
4. 用最小 `Config` 结构替换全局配置。

### 第三阶段：改善发布和运维

1. 固定可重复的 Linux/Windows 构建入口；
2. 补充最小离线测试，不依赖真实账号和校园网；
3. 明确 systemd 模板与最终 unit 的关系；
4. 文档中区分“编译验证”“真实登录验证”和“部署验证”。

## 9. 不建议现在做的事

- 不建议引入跨平台 GUI 框架；
- 不建议引入完整 IoC、插件或模块系统；
- 不建议新增独立网络在线探测服务；
- 不建议为了少量配置键引入大型配置平台；
- 不建议在没有真实失败样例前实现复杂指数退避、连接池或后台任务框架；
`net: -150~350 lines, -0 dependencies possible.`
